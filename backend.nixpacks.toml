[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm ci --prefix backend",
  "npm ci"
]

[phases.build]
cmds = [
  "npx prisma generate",
  "cd backend && npx prisma generate --schema ../prisma/schema.prisma"
]

[start]
cmd = "cd backend && node --import tsx src/index.ts"
