// Full booking details — identical resolver to [id]/route.ts
// Exposed as /api/admin/bookings/{reference}/full-details
// Accepts: DB id, masterBookingReference, masterPnr, or any BookingReference value
export { GET } from '../route';
